# 原始資料（不提交檔案）

這個目錄只保留規則說明。原始錄影、語音、問卷匯出與 log 受 `.gitignore` 保護，請放在私有研究儲存空間。

每份原始資料至少在 `data/templates/video_manifest.csv` 或 `data/evidence_index.csv` 記錄：

- 唯一 ID。
- 場次 ID。
- 原始檔名。
- 私有儲存位置代稱。
- SHA-256。
- 建立日期。
- 是否含語音或個資。
- 誰有權存取。
- 是否已有去識別副本。

原始檔只讀，不直接剪輯或覆寫。所有剪輯與轉檔從工作副本產生。
